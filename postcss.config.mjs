/**
 * Integrazione Tailwind CSS 4 con PostCSS: elabora le direttive di globals.css
 * e genera le utility usate dalle pagine e dai componenti.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
